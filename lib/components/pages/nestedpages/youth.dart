import 'package:flutter/material.dart';

class YouthPage extends StatelessWidget {
  const YouthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Youth Page'),
        backgroundColor: Colors.blue,
      ),
    );
  }
}
